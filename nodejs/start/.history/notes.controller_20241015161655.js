const notes = []

function addNotes() {
    const note = {
        title,
        id: Date.now().toString()
    }

    notes.push()
}

function getNotes() {}

module.exports = {
    addNotes, getNotes

}