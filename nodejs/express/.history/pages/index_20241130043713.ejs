<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        ;
        .btn {
            width: 100px; 
            height: 40px; 
        }
        input {
            width: 150px; /* Задайте фиксированную ширину */
        }

        /* Кнопки остаются на месте, и инпут не растягивает контейнер */
        .btn_box {
            display: flex;
            gap: 10px;
            align-items: center;
        }
    </style>
    <title><%= title %></title>
</head>
<body>
    <% if(created) {%>
        <div class="alert alert-success">
            Note has been created
        </div>
        <% }%>
    <div class="container">
        <h1><%= title %></h1>
        
        <form action="/" method="post">
            <div class="form-group mb-2">
                <input type="text" class="form-control" name="title" required >
            </div>
            <button class="btn btn-primary" type="submit">
                Create
            </button>
        </form>

        <% if(notes.length) { %>
        <div class="card">
            <ul class="list-group">
                <% for (let i = 0; i < notes.length; i++) {%>
                    <li class="list-group-item d-flex justify-content-between align-items-center" data-id="<%= notes[i].id%>">
                        <div class="flex-grow-1 text-truncate">
                            <p class="list-title mb-0"><%= notes[i].title %></p>
                        </div>
                        <div class="btn_box ms-2">
                            <button class="btn btn-primary" data-type="edit" data-id="<%= notes[i].id%>">Edit</button>
                            <button class="btn btn-danger" data-type="remove" data-id="<%= notes[i].id%>">&times;</button>
                        </div>
                    </li>
                    
                <% } %>
            </ul>
        </div>
        <% } else { %>
            <p>No notes</p>
        <% } %>
    </div>
    <script src="/app.js"></script>
</body>
</html>