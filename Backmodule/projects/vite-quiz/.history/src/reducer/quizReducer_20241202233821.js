// import { getQuestions } from "../utils/utils"
import { ACTION_TYPE } from "../actions";

const initialState = {
    questions: [],
    loading
}
const quizReducer = () => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}