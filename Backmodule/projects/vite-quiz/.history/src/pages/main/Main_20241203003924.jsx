import { loadSuccess } from "../../actions"
import { useDispatch, UseSelector } from "react-redux"

export const Main = () => {
    const dispatch = useD
    return (
        <>
            <h1>Quiz-Test</h1>
        </>
    )
}