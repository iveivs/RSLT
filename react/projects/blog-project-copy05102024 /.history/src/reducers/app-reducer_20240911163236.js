const initialAppState = {

}

export const appReducer = (state = const initialPostsState = {

}

export const postsReducer = (state = initialPostsState, action) => {
    switch (action.type) {
    
        default:
            return state;
    }
} , action) => {
    switch (action.type) {
    
        default:
            return state;
    }
} 