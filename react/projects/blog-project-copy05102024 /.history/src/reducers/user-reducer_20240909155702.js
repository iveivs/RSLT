const initialUserState = {

}

export const userReducer = (state = initialUserState, action) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
} 