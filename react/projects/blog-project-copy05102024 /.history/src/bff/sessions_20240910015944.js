export const sessions = {
    list: {},
    create(user) {
        const hash = Ma;

        this.list[hash] = user  
        return hash
    },
    add(hash, user) {
        
    },
    remove() {},
}