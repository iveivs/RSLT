import styled from "styled-components";
const H2Container = ({ children, className}) => {
    return (
        <h2 className={className}>
            {children}
        </h2>
    )
}

    margin: 40px 0;
`