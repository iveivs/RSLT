import { styled } from "styled-components";
import { Input, Button, H2, AuthFormError } 

const UsersContainer = ({ className }) => {
    return (    
        <div className={className}>
            
        </div>

    )
}

export const Users = styled(UsersContainer)`

`