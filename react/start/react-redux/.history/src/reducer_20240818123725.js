export const initialState = {
    user: {
        name: 'Martin',
        age: 123,
    },
    products: [],
}

export const reducer = (state = in) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}