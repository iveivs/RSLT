export const initialState = {
    user: {
        name: 'Martin',
        age: 123,
    },
    products: [],
}

export const reducer = (state = initialState, aaction) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}