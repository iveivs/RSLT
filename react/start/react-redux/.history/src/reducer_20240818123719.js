export const initialState = {
    user: {
        name: 'Martin',
        age: 123,
    },
    products: [],
}

export const reducer = (this.state.) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}