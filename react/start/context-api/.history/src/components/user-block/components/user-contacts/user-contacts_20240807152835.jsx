export const UserContacts = ({ id, phone, email }) => {
    console.log(object);
    return (
        <>
            <h3>Контакты: </h3>
            <div>Почта: {email}</div>
            <div>Телефон: {phone}</div>
        </>
    )
}