import { InformationLayout } from './informationLayout';

const Information = ({state}) => {
    return (
        <InformationLayout 
            state={state}
        />
    );
};

export default Information;

